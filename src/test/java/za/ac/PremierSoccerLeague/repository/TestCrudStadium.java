//package za.ac.PremierSoccerLeague.repository;

/**
 * Created by student on 2015/05/02.
 */
//public class TestCrudStadium {
   /* private Long id;
    
    @Autowired
    StadiumRepository repository;

    @Test
    public void testCreate() throws Exception {
        List<Stadium> stadiums = new ArrayList<Stadium>();
        Stadium stadium = new Stadium.Builder("Soccer Citty").location("Soweto").build();
        repository.save(stadium);
        Assert.assertNotNull(stadium.getId());
    }

    @Test
    public void testRead() throws Exception {
        Stadium stadium = repository.findOne(id);
        Assert.assertEquals("Soccer City", stadium.getName());
    }

    @Test
    public void testUpdate() throws Exception {
        Stadium stadium = repository.findOne(id);
        Stadium newStadium = new Stadium.Builder("Moses Mabida").location("Durban").build();
        repository.save(stadium);
        Assert.assertEquals("Moses Mabida", stadium.getName());
        Assert.assertEquals("Durban", stadium.getLocation());
    }

    @Test
    public void testDelete() throws Exception {
        Stadium stadium = repository.findOne(id);
        repository.delete(stadium);
        Stadium newStadium = repository.findOne(id);
        Assert.assertNull(newStadium);
    }*/
//}
