//package za.ac.PremierSoccerLeague.repository;

/**
 * Created by student on 2015/05/02.
 */
//public class TestCrudSponsor {
   /* private Long id;

    @Autowired
    SponsorRepository repository;

    @Test
    public void testCreate() throws Exception {
        List<Player> players = new ArrayList<Player>();

        Sponsor sponsor = new Sponsor.Builder().name("PUMMA")
                .sponsors("Ronaldo").build();
        repository.save(sponsor);
        id = sponsor.getId();
        Assert.assertNotNull(sponsor.getId());
    }

    @Test
    public void testRead() throws Exception {
        Sponsor sponsor = repository.findOne(id);
        Assert.assertEquals("PUMMA", sponsor.getName());
    }

    @Test
    public void testUpdate() throws Exception {
        Sponsor sponsor = repository.findOne(id);
        Sponsor newSponsor = new Sponsor.Builder().name("NIKE").sponsors("MESSI").build();
        repository.save(newSponsor);
        Assert.assertEquals("PUMMA", sponsor.getName());
        Assert.assertEquals("MESSI", sponsor.getSponsors());
    }

    @Test
    public void testDelete() throws Exception {
        Sponsor sponsor = repository.findOne(id);
        repository.delete(sponsor);
        Sponsor newSponsor = repository.findOne(id);
        Assert.assertNull(newSponsor);
    }*/
//}
